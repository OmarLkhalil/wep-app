let num = document.querySelector("span");
let num1 = document.querySelector("p:after");
console.log(num1);
    
/* let increment =function (){
    setTimeout(()=>{
        num.textContent = parseInt(num.textContent)+1},1000);
} */

/* const sleepNow = async function(delay){ new Promise((resolve)=>setTimeout(resolve,delay))};
    for (let i=0 ; i<10; i++){
        sleepNow(2000);
        console.log('hello');
    };  */
 
/* while(parseInt(num.textContent)<10){
    num.textContent = parseInt(num.textContent+1);
    
}
console.log(parseInt(num.textContent)+1); */
/* let counter =setInterval(()=>{ 
      
     num.textContent = parseInt(num.textContent)+1;
    if(parseInt(num.textContent)===10){
        clearInterval(counter);
    }
});  */
 
/* for (let i=0 ; i<10; i++){
    setInterval(()=>{
        while( parseInt(num.textContent)+1<i+1){
        num.textContent = parseInt(num.textContent)+1; }
    }),1000;
    
    console.log(parseInt(num.textContent)+1);
} */

/* console.log(parseInt(num.innerHTML+1)); */